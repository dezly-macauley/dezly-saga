
fn main() {
    //_________________________________________________________________________ 
    // SECTION: Arrays
    // 1. A collection of objects of the same type
    // 2. The objects are stored in contiguous memory (next to each other)
    // 3. The length of an array must be known at compile time
    // 4. Stored on the stack (you can also say allocated on the stack)

    let my_array: [i32; 3] = [16, 26, 7];
    println!("My array is {:?}", my_array);
    //_________________________________________________________________________ 
    // SECTION: Slice
   

    //_________________________________________________________________________ 
}
