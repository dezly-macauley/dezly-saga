export litecli_config_name="user_database"
export litecli_config_path="./.litecli_configs/$litecli_config_name"

# NOTE: The line below has nothing to do with `direnv`
# This is simply to tell Neovim to treat this a zsh file, 
# .envrc is not a recognized file type.

# vi: ft=zsh
