// NOTE: Enum

// This is basically like a radio button.
// An enum is a variable that has a limited number of options but only one of
// those options can be selected at a given time.

const Direction = enum { north, south, east, west };
